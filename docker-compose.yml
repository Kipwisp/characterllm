services:
  characterllm:
    image: ghcr.io/lutravox/characterllm:latest
    env_file: .env
    restart: unless-stopped
    volumes:
      - data:/app/data
      - logs:/app/logs

volumes:
  data:
  logs:
